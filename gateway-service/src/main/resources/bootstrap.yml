server:
  port: 7002

spring:
  application:
    name: gateway-service
  cloud:
    config:
      uri: http://localhost:9999/        #Config server的uri
      profile: dev                       #指定的环境
      label: master                      #指定分支

